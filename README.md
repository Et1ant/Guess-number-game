# Guess the Number — Игра «Угадай число»

##  Обзор проекта

Консольная игра «Угадай число» с тремя уровнями сложности, системой попыток и возможностью многократной игры. Проект демонстрирует работу со словарями, обработку пользовательского ввода, случайную генерацию чисел и организацию игровой логики.

##  Функциональность

- **Три уровня сложности** — легкий, средний, сложный
- **Разные диапазоны чисел** — от 1-50 до 1-200
- **Разное количество попыток** — от 5 до 10
- **Система подсказок** — «больше» / «меньше»
- **Повторная игра** — без перезапуска программы
- **Защита от некорректного ввода** — обработка ошибок

## 🛠 Технологии

- Python 3.x
- Модуль `random` для генерации случайных чисел
- Консольный интерфейс
- Обработка исключений

##  Структура проекта

```
guess-number-game
│
├── learn.py          # Основной файл игры
└── README.md        # Документация
```

## 💻 Детальный разбор кода

### 1. Импорт модулей

```python
import random
```

Импорт модуля для генерации случайных чисел. `random.randint()` будет использоваться для создания загаданного числа.

---

### 2. Функция выбора сложности

```python
def choose_difficulty():
    """Выбор уровня сложности"""
    print("\nВыберите уровень сложности:")
    print("1 - Легкий (1-50, 10 попыток)")
    print("2 - Средний (1-100, 7 попыток)")
    print("3 - Сложный (1-200, 5 попыток)")
    
    while True:
        choice = input("Введите номер (1-3): ").strip()
        if choice in ['1', '2', '3']:
            return int(choice)
        print(" Ошибка! Введите 1, 2 или 3")
```

**Как работает:**
1. Выводит меню с описанием всех уровней сложности
2. Запрашивает ввод пользователя
3. Проверяет, является ли ввод одним из допустимых значений ('1', '2', '3')
4. При успехе возвращает число, при ошибке — повторяет запрос

**Ключевые особенности:**
- `strip()` удаляет лишние пробелы
- Проверка через `in` для нескольких значений
- Бесконечный цикл с условием выхода
- Немедленная обратная связь об ошибке

---

### 3. Функция настроек сложности

```python
def get_difficulty_settings(level):
    """Возвращает настройки для выбранного уровня"""
    settings = {
        1: {'min': 1, 'max': 50, 'attempts': 10, 'name': 'Лёгкий'},
        2: {'min': 1, 'max': 100, 'attempts': 7, 'name': 'Средний'},
        3: {'min': 1, 'max': 200, 'attempts': 5, 'name': 'Сложный'}
    }
    return settings[level]
```

**Архитектурное решение:**
- Использование словаря для хранения настроек
- Ключи — номера уровней (1, 2, 3)
- Значения — вложенные словари с параметрами
- Централизованное управление конфигурацией

**Преимущества:**
- Легко добавлять новые уровни
- Удобный доступ по ключу
- Читаемая структура данных

---

### 4. Функция безопасного ввода числа

```python
def get_number_input(min_num, max_num):
    """Безопасный ввод числа с проверкой"""
    while True:
        try:
            num = int(input(f"Введите число от {min_num} до {max_num}: "))
            if min_num <= num <= max_num:
                return num
            print(f" Число должно быть от {min_num} до {max_num}!")
        except ValueError:
            print(" Ошибка! Введите целое число.")
```

**Многоуровневая защита:**

| Уровень | Проверка | Реакция на ошибку |
|---------|----------|-------------------|
| 1 | Преобразование в `int` | `ValueError` → сообщение |
| 2 | Диапазон значений | Вне диапазона → сообщение |
| 3 | Тип данных | Уже обработан |

**Особенности:**
- Динамическое формирование строки с диапазоном
- `try-except` для перехвата ошибок преобразования
- Информативные сообщения об ошибках

---

### 5. Основная игровая функция

```python
def play_game():
    """Основная функция игры"""
    print("=" * 40)
    print(" ИГРА 'УГАДАЙ ЧИСЛО'")
    print("=" * 40)
    
    while True:
        start = input("\nНачать игру? (да/выход): ").strip().lower()
```

#### 5.1 Внешний цикл (управление игрой)

```python
if start == 'выход':
    print("\nСпасибо за игру! До свидания! ")
    break
elif start == 'да':
    # ... игровая логика ...
else:
    print(" Неверный ввод. Пожалуйста, введите 'да' или 'выход'.")
```

**Структура:**
- Бесконечный цикл для многократных игр
- Три варианта ответа: `да`, `выход`, невалидный ввод
- `break` для полного выхода из программы

#### 5.2 Инициализация игры

```python
# Выбор сложности
level = choose_difficulty()
settings = get_difficulty_settings(level)

print(f"\n Уровень: {settings['name']}")
print(f" Диапазон: {settings['min']}-{settings['max']}")
print(f" Попыток: {settings['attempts']}")

# Генерация числа
secret_number = random.randint(settings['min'], settings['max'])
attempts_left = settings['attempts']
```

**Последовательность:**
1. Пользователь выбирает уровень
2. Загружаются соответствующие настройки
3. Выводится сводка параметров
4. Генерируется случайное число
5. Устанавливается счётчик попыток

#### 5.3 Внутренний цикл (процесс угадывания)

```python
while attempts_left > 0:
    print(f"\nОсталось попыток: {attempts_left}")
    guess = get_number_input(settings['min'], settings['max'])
    attempts_left -= 1
    
    if guess < secret_number:
        print(" Ваше число МЕНЬШЕ загаданного")
    elif guess > secret_number:
        print(" Ваше число БОЛЬШЕ загаданного")
    else:
        # ПОБЕДА!
        print("=" * 40)
        print(f" ПОБЕДА! Вы угадали число {secret_number}!")
        print(f" Использовано попыток: {settings['attempts'] - attempts_left}")
        print("=" * 40)
        break
```

**Логика победы/поражения:**
- **Победа:** совпадение чисел → вывод статистики → `break`
- **Поражение:** `attempts_left == 0` → открытие загаданного числа

#### 5.4 Обработка поражения

```python
if attempts_left == 0:
    print("=" * 40)
    print(f" Игра окончена! Вы исчерпали все попытки.")
    print(f" Загаданное число было: {secret_number}")
    print("=" * 40)
    break
```

**Важно:** Эта проверка находится внутри цикла, но выполняется только при исчерпании попыток и отсутствии победы.

---

### 6. Точка входа

```python
if __name__ == "__main__":
    play_game()
```

Стандартная идиома Python:
- При прямом запуске → игра начинается
- При импорте → код не выполняется автоматически

---

##  Установка и запуск

1. Создайте файл `guess_number.py`
2. Скопируйте код в файл
3. Запустите игру:

```bash
python guess_number.py
```

---

## Пример игровой сессии

```
========================================
 ИГРА 'УГАДАЙ ЧИСЛО'
========================================

Начать игру? (да/выход): да

Выберите уровень сложности:
1 - Легкий (1-50, 10 попыток)
2 - Средний (1-100, 7 попыток)
3 - Сложный (1-200, 5 попыток)
Введите номер (1-3): 2

 Уровень: Средний
 Диапазон: 1-100
 Попыток: 7

Я загадал число! Попробуйте угадать!

Осталось попыток: 7
Введите число от 1 до 100: 50
 Ваше число МЕНЬШЕ загаданного

Осталось попыток: 6
Введите число от 1 до 100: 75
 Ваше число БОЛЬШЕ загаданного

...

========================================
 ПОБЕДА! Вы угадали число 73!
 Использовано попыток: 4
========================================
```

---

##  Изученные концепции Python

| Концепция | Применение в проекте |
|-----------|---------------------|
| **Словари** | Хранение настроек уровней сложности |
| **Циклы** | `while True` для повторных игр и валидации |
| **Условные операторы** | Проверка ввода, сравнение чисел |
| **Функции** | Декомпозиция на 4 функции |
| **Обработка исключений** | `try-except` для числового ввода |
| **Модуль random** | `random.randint()` для генерации числа |
| **F-строки** | Динамическое формирование вывода |
| **Срезы строк** | `"=" * 40` для разделителей |

---

##  Возможные улучшения

###  Статистика
- [ ] Таблица рекордов
- [ ] Лучшие результаты по уровням
- [ ] Процент побед

###  Интерфейс
- [ ] Цветной вывод (`colorama`)
- [ ] Анимация попыток
- [ ] Графическая шкала прогресса

###  Геймплей
- [ ] Подсказка за половину попыток
- [ ] Бесконечный режим
- [ ] Режим 2 игроков
- [ ] Звуковые эффекты

###  Данные
- [ ] Сохранение статистики в файл
- [ ] Настраиваемые уровни
- [ ] Случайный диапазон чисел

---

##  FAQ

**Q: Что происходит при вводе букв вместо чисел?**  
A: Срабатывает `ValueError`, выводится сообщение об ошибке, запрос повторяется.

**Q: Можно ли выйти во время игры?**  
A: Только через главное меню. В текущей версии нужно закончить раунд.

**Q: Почему используется словарь для настроек?**  
A: Это масштабируемое решение — легко добавить 4-й уровень без изменения логики.




